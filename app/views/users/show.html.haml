%h2
	= @user.username

%p
	%b Account Type:
	= @user.account_type

%p #{@user.username}'s seedlings

#user_seedlings
	- @user.seedlings.each do |seedling|
		%a{:href => seedling_path(seedling)}
			%img{:src => seedling.picture.url(:thumb)}/